#
# Admin partial templates are private.
#
# They are included by the authenticated admin shell and should not be reachable
# as standalone browser resources.
#
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
