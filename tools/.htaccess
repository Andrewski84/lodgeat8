#
# Maintenance tools are CLI-only.
#
# Deployment checks should be run from the command line and must not be exposed
# as public web endpoints.
#
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
